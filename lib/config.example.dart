// Copy this file to config.dart and fill in your server's values.
// lib/config.dart is gitignored — never commit your real IP or secret.

// Your server PC's local IP address (run `ipconfig` to find it) + port
const String kServerUrl = 'http://192.168.1.100:3000';

// Must match SERVER_SECRET in server/.env
const String kServerSecret = 'change-this-to-match-your-server';
